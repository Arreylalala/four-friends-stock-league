# 项目维护规则

## 开始工作

- 本仓库是华尔街之狼股票战报的正式源码，远程为 `Arreylalala/four-friends-stock-league`。
- 阅读 `CHANGELOG.md`；如果 `../../HANDOFF.md` 存在，也完整阅读它以了解工作区现状。
- 修改前检查 Git 工作区和 GitHub 最新代码、`data/` 数据文件。保留用户未提交的修改和更新的数据。

## 每次修改都更新 CHANGELOG.md

这是一条适用于维护代理的执行规则，不是文件监听器或 GitHub 自动任务。

- 修改代码、数据、配置、测试或文档时，在交付前同步更新 `CHANGELOG.md`，无需用户再次提醒。
- 一次逻辑上完整的修改记录一条；不要按每次保存、每次工具调用或临时尝试生成记录。
- 记录放在最上方，注明北京时间日期、改动内容及原因、实际验证结果和交付状态。
- 状态明确区分：本地未提交、已提交未推送、已推送待部署、已部署成功、失败/阻塞。
- 只有得到 GitHub Actions 成功及线上核对证据后，才能标记已部署成功。尽量附提交或工作流链接。
- 无法验证时写明未验证及原因；不得复制过去测试结果冒充本次验证。
- 用户明确要求记录之外的处理方式时，遵循用户指令。用户并未授权发布的任务，不要为了变更记录擅自发布。
- 提交已授权的修改时，将对应记录与修改一起提交。首次提交前可写“随本次提交交付”，不要求记录引用自身的提交哈希。
- 发布后的状态补充属于同一条记录，不必创建重复条目，也不要为了记录其自身提交/部署状态制造无休止的提交。
- 只读查询、讨论和没有产生持久修改的检查无需新增记录。历史补录应标注来源，不要补造不存在的测试或发布时间。
- 不记录密钥、令牌、私人登录信息；每日完整盈亏数据保留在按月 JSON，变更记录只写日期范围和必要更正说明。

## HANDOFF.md 的更新时机

- 日常小改动主要写 CHANGELOG，不需要重复重写交接文档。
- 工作目录、正式分支、部署方式、数据规则、关键约束或阻塞状态改变时，同步更新可用的 `../../HANDOFF.md`。
- 新的独立克隆可能没有工作区 HANDOFF；此时以本仓库 README、AGENTS、CHANGELOG 和实际代码为准。

## 验证和发布约束

- `data/` 数据文件为唯一正式数据源，public 副本由脚本生成；不可用旧数据覆盖远程最新记录。
- 功能或数据变化按影响执行 `pnpm check`；纯文档修改核对事实、路径和 Git diff 即可。
- 只有 main 通过 Actions 测试、构建和导出检查后才部署。无需上传 ZIP。
- 连胜徽章使用当前连胜，至少 2 场显示为 `2连胜`；生产资源保留 `/four-friends-stock-league/` 子路径。
- GitHub 凭据和提交身份保持仓库级配置，不改动全局 Git 或工作 GitLab 配置。
